import sys
import os

# TODO: open file to be read in binary format 
# with open(data_file, "r+b") as read_file:
    # TODO: set the file obj pointer to the end of the file
    #  data_file.seek(0, os.SEEK_END)
    #
    # TODO: set the current position
    # position = file.tell() 
    #
    # TODO: conditional to check if we're past the 
    # current position that was previously set.
        # TODO: start writing data to screen